---

services:
  printer-testbed:
    platform: linux/arm/v7
    build:
      context: ..
      dockerfile: e2e/Dockerfile
      platforms:
        - linux/arm/v7
    container_name: ack2-e2e-testbed
    hostname: printer-testbed
    ports:
      - "20022:22"   # SSH access for deployment
      - "20080:80"   # Webserver HTTP access
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "nc", "-z", "localhost", "22"]
      interval: 1s
      timeout: 1s
      retries: 5
      start_period: 2s
    networks:
      - printer-net

networks:
  printer-net:
    driver: bridge