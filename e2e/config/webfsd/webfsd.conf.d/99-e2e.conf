# E2E Testbed override configuration
# Loaded after main config to customize for testing environment
#
# This file overrides settings from /etc/webfsd/webfsd.conf

# Log file: E2E uses /mnt/UDISK/log (no printer app running)
-l /mnt/UDISK/log
